#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.13.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from service import gate
from service.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  void set_context(string conn_id, string key, string value)')
    print('  void unset_context(string conn_id, string key, string value)')
    print('  void remove_conn(string conn_id)')
    print('  void send_text(string conn_id, string message)')
    print('  void send_binary(string conn_id, string message)')
    print('  void join_group(string conn_id, string group)')
    print('  void leave_group(string conn_id, string group)')
    print('  void broadcast_binary(string group,  exclude, string message)')
    print('  void broadcast_text(string group,  exclude, string message)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = gate.Client(protocol)
transport.open()

if cmd == 'set_context':
    if len(args) != 3:
        print('set_context requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_context(args[0], args[1], args[2],))

elif cmd == 'unset_context':
    if len(args) != 3:
        print('unset_context requires 3 args')
        sys.exit(1)
    pp.pprint(client.unset_context(args[0], args[1], args[2],))

elif cmd == 'remove_conn':
    if len(args) != 1:
        print('remove_conn requires 1 args')
        sys.exit(1)
    pp.pprint(client.remove_conn(args[0],))

elif cmd == 'send_text':
    if len(args) != 2:
        print('send_text requires 2 args')
        sys.exit(1)
    pp.pprint(client.send_text(args[0], args[1],))

elif cmd == 'send_binary':
    if len(args) != 2:
        print('send_binary requires 2 args')
        sys.exit(1)
    pp.pprint(client.send_binary(args[0], args[1],))

elif cmd == 'join_group':
    if len(args) != 2:
        print('join_group requires 2 args')
        sys.exit(1)
    pp.pprint(client.join_group(args[0], args[1],))

elif cmd == 'leave_group':
    if len(args) != 2:
        print('leave_group requires 2 args')
        sys.exit(1)
    pp.pprint(client.leave_group(args[0], args[1],))

elif cmd == 'broadcast_binary':
    if len(args) != 3:
        print('broadcast_binary requires 3 args')
        sys.exit(1)
    pp.pprint(client.broadcast_binary(args[0], eval(args[1]), args[2],))

elif cmd == 'broadcast_text':
    if len(args) != 3:
        print('broadcast_text requires 3 args')
        sys.exit(1)
    pp.pprint(client.broadcast_text(args[0], eval(args[1]), args[2],))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
